import { IsInTheFuturePipe } from './is-in-the-future.pipe';

describe('IsInTheFuturePipe', () => {
  it('create an instance', () => {
    const pipe = new IsInTheFuturePipe();
    expect(pipe).toBeTruthy();
  });
});
